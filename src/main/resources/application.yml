server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/adamY97/Bootcamp-ConfigServer
          default-label: main
          search-paths:
          - '*service'